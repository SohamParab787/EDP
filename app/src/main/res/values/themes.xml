<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EDP20" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimaryVariant">@color/appBackground</item>
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
    </style>

    <style name="Theme.EDP20" parent="Base.Theme.EDP20" />
    <style name="exampleToast">
        <item name="stTextColor">@color/white</item>
        <item name="stFont">@font/poppins_semibold</item>
        <item name="stColorBackground">#228B22</item>
        <item name="stIconStart">@drawable/check</item>
        <item name="stLength">SHORT</item>

    </style>
    <style name="exampleToast1">
        <item name="stTextColor">@color/white</item>
        <item name="stFont">@font/poppins_semibold</item>
        <item name="stColorBackground">#f01e2c</item>
        <item name="stIconStart">@drawable/clear</item>
        <item name="stLength">SHORT</item>

    </style>
</resources>